apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-pvc
  namespace: arturo-llm-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard  # clase estándar en GKE
  resources:
    requests:
      storage: 20Gi
