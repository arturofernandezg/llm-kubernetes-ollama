# Sesion de trabajo — 2026-03-23

## Estado al iniciar
- Agent desplegado con imagen `aiops-agent:5f64b61` (103 tests, webhook activo)
- Webhook `/webhook/alert` responde 422 si falta `startsAt` → validación Pydantic OK
- Mattermost + PostgreSQL running en `arturo-mattermost`
- ChromaDB en CrashLoopBackOff (no bloquea Fase 1)
- Permisos ClusterRole pedidos al tutor (pendiente respuesta)

## Tareas de la sesión (en orden de prioridad)

### Tarea 1 — Test end-to-end del webhook con payload completo
**Qué**: Enviar un curl con `startsAt` al webhook y verificar que procesa correctamente.
**Cómo**: El usuario ejecuta curl en Cloud Shell con port-forward activo.
**Criterio**: Respuesta 200 con JSON procesado.
**Archivo**: Ninguno que modificar, solo verificar.

### Tarea 2 — Configurar webhook entrante en Mattermost
**Qué**: Crear incoming webhook en Mattermost, obtener URL, configurar env var en el deployment del agente.
**Cómo**:
  1. Acceder a Mattermost (port-forward 8065)
  2. Crear incoming webhook en System Console o Integrations
  3. Copiar URL del webhook
  4. Añadir `MATTERMOST_WEBHOOK_URL` como env var en el deployment K8s
**Criterio**: Alertas llegan a canal de Mattermost.
**Archivos**: `k8s/agent.yaml` o `k8s/deployment.yaml` (env var)

### Tarea 3 — Investigar y arreglar ChromaDB CrashLoopBackOff
**Qué**: `kubectl logs chromadb-0 -n arturo-llm-test --tail=50` para ver por qué falla.
**Cómo**: Diagnóstico basado en logs. Posibles causas:
  - Recursos insuficientes en spot nodes
  - Error de inicialización de storage
  - Imagen incorrecta o falta de configuración
**Criterio**: Pod Running.
**Archivos**: `k8s/chromadb.yaml` (si hay que ajustar recursos/config)

### Tarea 4 — Test end-to-end completo: Alertmanager → Agent → Mattermost
**Qué**: Verificar flujo real completo sin RAG (por ahora).
**Cómo**:
  1. Enviar alerta a Alertmanager
  2. Alertmanager enruta al agente
  3. Agente formatea y envía a Mattermost
  4. Mensaje aparece en canal
**Criterio**: Mensaje visible en Mattermost.
**Dependencia**: Tareas 1 y 2 completadas.

### Tarea 5 (si da tiempo) — Definir alerting rules en ConfigMap
**Qué**: Crear reglas de alerta básicas para KubePodOOMKilled, CrashLoopBackOff, TargetDown.
**Cómo**: Sin Prometheus operator → definirlas en el ConfigMap de Alertmanager o esperar a kube-prometheus-stack (depende de permisos del tutor).
**Archivos**: `k8s/alertmanager.yaml`

## Notas
- Tests se ejecutan en Cloud Shell (no hay Python local)
- Commit después de cada tarea si hay cambios en código
- Permisos ClusterRole pendientes → Tarea 5 puede quedar bloqueada
