# Sesión 2026-03-30 — Setup + Plan Fase 2

## Contexto

Primera sesión en el Mac personal. Repo clonado como sandbox sin acceso a GCP/GKE.

## Lo que se hizo

### 1. Setup del entorno local
- Creado venv con Python 3.11 en `agent/.venv/`
- Instaladas dependencias de producción y desarrollo
- **103/103 tests pasando en 6.84s** — base verificada
- Fix menor: añadido `asyncio_default_fixture_loop_scope = function` en `pytest.ini` para eliminar deprecation warning de pytest-asyncio

### 2. Análisis del proyecto
- Leídos todos los módulos: `main.py`, `config.py`, `schemas.py`, `rag.py`, `diagnosis.py`, `mattermost.py`, `extraction.py`, `validation.py`
- Leídos docs: `01-architecture.md`, `07-roadmap.md`
- Mapeado el estado real de cada fase (ver esquema abajo)

### 3. Documentación de workflow
- Creado `docs/08-workflow-dual-env.md` — guía del ciclo Mac↔PC empresa
- Definido el patrón DEPLOY_CHECKLIST.md para cada tanda de trabajo

### 4. Plan de Fase 2 completo
Diseñado el plan multi-sesión para completar la Fase 2 (ver abajo).

---

## Plan Fase 2 — Desglose en sesiones

### Sesión 2 — Runbooks semilla
**Objetivo**: crear el contenido que alimenta el RAG.

- Diseñar formato de runbook (texto + metadata que espera `ingest_runbook()`)
- Escribir 15-18 runbooks para alertas K8s comunes:
  - OOMKilled, CrashLoopBackOff, ImagePullBackOff, HighCPU, HighMemory
  - PodEvicted, NodeNotReady, DiskPressure, NetworkUnavailable
  - TargetDown, KubeCPUOvercommit, KubeMemoryOvercommit
  - PodNotReady, ContainerWaiting, JobFailed, PersistentVolumeFillingUp
- Escribir función de ingesta batch (`ingest_all_runbooks()`)
- Tests para ingesta batch
- Guardar runbooks en `agent/runbooks/` como YAML

### Sesión 3 — Integrar RAG en el webhook ✅
**Completada**: pipeline RAG+LLM integrado en webhook con triple fail-open.
- 124/124 tests passing (+10 nuevos).
- Ver detalle en `2026-03-30-s3-rag-webhook.md`.

### Sesión 4 — `remediation.py` + validation layer
**Objetivo**: pasar de "monitor inteligente" a "agente autónomo".

- `remediation.py`: validation layer (whitelist/blacklist de comandos K8s)
- Auto-patch para casos seguros: `risk=low` + `confidence>=0.8` + cambio <25%
- Escalación a humano vía Mattermost para el resto
- RBAC least-privilege en K8s (Role + RoleBinding)
- Tests del módulo completo

### Sesión 5 — Deploy cluster + E2E (PC empresa)
**Objetivo**: evidencia real de que funciona desplegado.

- Preparar DEPLOY_CHECKLIST.md con todos los pasos
- En PC empresa:
  - `kubectl apply -f k8s/chromadb.yaml` (imagen 0.6.3, fix ya preparado)
  - Cargar `nomic-embed-text` en Ollama
  - Ingestar runbooks en ChromaDB
  - Cloud Build nueva imagen (124+ tests como gate)
  - Test E2E: curl con payload Alertmanager → diagnóstico en Mattermost (con screenshot)

### Sesión 6 — Feedback loop + evaluación
**Objetivo**: cerrar el ciclo de aprendizaje del sistema.

- Persistir incidentes resueltos en colección `incidents` de ChromaDB
- Evaluación offline: datasets de alertas simuladas vs ground truth
- Métricas: MTTR, retrieval precision, actionability rate, safety rate

---

## Estado de fases (snapshot)

```
Fase 0 (Terraform)         ██████████ 100% ✅
Fase 1 (Observabilidad)    ████████░░  ~85% (pendiente: webhook Mattermost, alerting rules, e2e)
Fase 2 (RAG)               ████████░░  ~80% (código completo + integrado, falta: deploy ChromaDB, cargar runbooks, e2e)
Fase 3 (Remediación)       ░░░░░░░░░░    0%
```

## Próxima sesión

Sesión 4: `remediation.py` + validation layer (ver plan arriba).
